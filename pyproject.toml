[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "picobot"
version = "0.1.1.1"
description = "A tiny bot built for simplicity"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
   "click",
   "langchain>=1.2.0,<=1.2.16",
   "langchain-community",
   "langchain-core>=1.3.0,<=1.3.2",
   "langchain-openai",
   "langgraph>=1.1.0,<=1.1.10",
   "rich",
]

[project.urls]
Homepage = "https://github.com/ngpeijiun/picobot"
Repository = "https://github.com/ngpeijiun/picobot"

[project.scripts]
picobot = "picobot.cli:chat"

[tool.setuptools.packages.find]
where = ["src"]
